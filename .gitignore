# ─── Segredos e configuração local (NUNCA versionar) ───────────────────────
# local.properties guarda sdk.dir e, a partir do M1, o PAT read:packages do DAT.
local.properties
*.keystore
*.jks
secrets.properties

# ─── Build ─────────────────────────────────────────────────────────────────
.gradle/
build/
**/build/
captures/
*.apk
*.aab

# ─── Android / modelos nativos ─────────────────────────────────────────────
# Modelos on-device (whisper ggml, piper/sherpa-onnx, silero, openWakeWord)
# são baixados pelo setup (ver README) e não entram no Git.
*.ggml
*.gguf
app/src/main/assets/models/

# ─── IDE ───────────────────────────────────────────────────────────────────
.idea/
*.iml
.kotlin/

# ─── macOS ─────────────────────────────────────────────────────────────────
.DS_Store

# Modelos de teste (baixados no setup, ver README)
app/src/androidTest/assets/models/

# NDK build intermediates
.cxx/

# AAR pré-compilado do sherpa-onnx (baixado no setup, ver README)
core-voice/libs/*.aar

# Arquivo de contexto da ferramenta de desenvolvimento (local, não versionado).
# O conteúdo canônico vive em docs/PADROES_DE_ENGENHARIA.md.
CLAUDE.md
