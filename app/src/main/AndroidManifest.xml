<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      M0: apenas o shell (launcher + deeplink de retorno). As permissões do DAT
      (BLUETOOTH, BLUETOOTH_CONNECT, INTERNET, RECORD_AUDIO, CAMERA), o
      foregroundServiceType e os meta-data APPLICATION_ID/CLIENT_TOKEN entram no
      M1/M8, sempre confirmados na doc viva. Ver DECISIONS.md e README.
    -->

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClaryonField">

        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              Retorno do app Meta AI após o registro dos óculos. Scheme EXCLUSIVO
              (claryonfield://), nunca genérico — evita prender o usuário no app
              Meta AI (armadilha conhecida). VIEW + DEFAULT + BROWSABLE.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="claryonfield" />
            </intent-filter>
        </activity>
    </application>
</manifest>
